---
import { getCollection } from 'astro:content'
import { render } from 'astro:content'
import FormattedDate from '../widgets/FormattedDate.astro'

const thoughts = await getCollection('thoughts')
// Get the latest thought
const latestThought = thoughts.sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
)[0]

if (!latestThought) {
  return null
}

const { Content } = await render(latestThought)
---

<a href="/thoughts" class="thought-section">
  <div class="section-header">
    <div class="thought-label">Recent Thought</div>
    <div class="thought-hint">View all →</div>
  </div>
  <div class="thought-divider"></div>
  <div class="thought-content">
    <Content />
  </div>
  <div class="thought-meta">
    <FormattedDate date={latestThought.data.pubDate} />
  </div>
</a>

<style>
  .thought-section {
    color: var(--text-primary);
    text-decoration: none;
    margin-bottom: 2rem;
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .thought-label {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .thought-hint {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    opacity: 0;
    transition: opacity 0.2s ease-out;
  }

  .thought-section:hover .thought-hint {
    opacity: 1;
  }

  @media (hover: none) or (pointer: coarse) {
    .thought-hint {
      opacity: 1;
    }
  }

  .thought-divider {
    height: 1px;
    background-color: var(--border);
    margin-bottom: 1rem;
  }

  .thought-link {
    display: block;
    text-decoration: none;
    transition: opacity 0.2s ease-out;
  }

  .thought-content {
    font-family: var(--sans);
    font-size: var(--font-size-m);
    line-height: 1.6;
    color: var(--text-primary);
    max-height: 8rem;
    overflow-y: auto;
    padding-right: 0.5rem;
    margin-bottom: 0.75rem;
    scrollbar-width: thin;
    scrollbar-color: var(--border) transparent;
  }

  .thought-content::-webkit-scrollbar {
    width: 6px;
  }

  .thought-content::-webkit-scrollbar-track {
    background: transparent;
  }

  .thought-content::-webkit-scrollbar-thumb {
    background-color: var(--border);
    border-radius: 3px;
    transition: background-color 0.2s ease-out;
  }

  .thought-content::-webkit-scrollbar-thumb:hover {
    background-color: var(--text-tertiary);
  }

  .thought-content :global(p) {
    margin: 0;
  }

  .thought-content :global(p + p) {
    margin-top: 0.5em;
  }

  .thought-meta {
    font-family: var(--mono);
    font-size: var(--font-size-xs);
    color: var(--text-tertiary);
    text-align: right;
    letter-spacing: var(--spacing-s);
  }
</style>
